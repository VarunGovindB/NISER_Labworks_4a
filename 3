#include <iostream>
using namespace std;
void max(int a, int b)
{   
int n;
n= (a > b ) ? a : b;
cout<<"Max:"<<n;
}
void min(int a, int b)
{
int n;
n= (a > b) ? b : a;
cout<<"Min:"<<n;
}
int main() 
{
int a, b;
cout<<"Enter any two numbers: ";
cin>>a>>b;
max(a,b);  
min(a, b);  
return 0;
}
